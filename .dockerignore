node_modules
dist
coverage
.git
.env
.env.*
!.env.example
*.log
# NOTE: `docs` is NOT ignored — prisma/seed.ts reads docs/api/provider/catalog-seed.json.
# It is only ever copied into the discarded build stage; the runtime image never gets it.
tests
